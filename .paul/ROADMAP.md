# Roadmap: Swimnetics

> **Historical phase index (Phases 1–77).** For what's in flight right now use
> [STATE.md](STATE.md), not this file. Per-phase detail (PLAN / SUMMARY / CONTEXT) lives in
> `.paul/phases/<n>-*/`. The pre-trim long-form roadmap is archived at
> `.paul/archive/ROADMAP-full-2026-08-21.md`.
>
> Status key: ✅ complete · 🚧 in progress · 📋 planned (scoped, not built) · ⏸ unscheduled TODO.
> A blank date means the completion date wasn't captured here — see the phase dir.

## Overview
End-to-end velocity-tracking and stroke-analysis pipeline for swimming. Answers questions like
"how does my technique vary throughout my swim?"

---

## Milestone: v0.1 Initial Release ✅

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 1 | Segmentation Refactor | ✅ | 2026-05-17 |
| 2 | Metric Explanations | ✅ | 2026-05-17 |
| 3 | BLE Record View | ✅ | 2026-05-20 |

## Milestone: v0.2 Coach Demo ✅
**Goal:** Live demo-ready iOS app — full analytical breakdown, multi-swimmer dashboard, dive/pulldown detection, per-athlete history.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 4 | FastAPI Backend | ✅ | 2026-05-20 |
| 5 | iOS App — TestFlight | ✅ | 2026-05-22 |
| 6 | Auth + Athlete Profiles | ✅ | 2026-05-23 |
| 7 | Algorithm + Backend | ✅ | 2026-05-23 |
| 8 | iOS Full Analytics | ✅ | 2026-05-24 |
| 9 | iOS Dashboard + History | ✅ | 2026-05-24 |

## Milestone: v0.3 Metrics Quality ✅
**Goal:** Coach-facing data-quality signals — cycle plausibility, magnet dropout, outlier counts, kick-reliability flags.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 10 | Metrics Quality | ✅ | 2026-05-25 |

## Milestone: v0.3.5 Pipeline Tests ✅
**Goal:** Pytest suite over the signal pipeline so metric/API changes don't silently regress.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 11 | Pipeline Tests | ✅ | 2026-05-25 |

## Milestone: v0.4 Coach Experience ✅
**Goal:** Coach QoL — session naming, notes, starring, deletion, stroke filter, pre-recording config.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 12 | QoL Features | ✅ | 2026-05-25 |

## Milestone: v0.4.5 Graph Enhancements ✅
**Goal:** Interactive velocity chart — time markers, touch cursor, pinch-zoom, m/yd toggle, live recording graph.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 13 | Graph Enhancements | ✅ | 2026-05-25 |

---

## Milestone: v0.5 Commercial Foundation 🚧
**Goal:** Billing, device management, website + parent reports, video ground truth, and the race-phase
report-card model — everything needed for paying customers. This is the active milestone; the
segmentation / phase-metrics work (Phases 75–77) is the current frontier.

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 14 | Device Registration | ✅ | 2026-06-08 |
| 15 | Billing + Tier Enforcement | ✅ | |
| 16 | Freestyle Support | ✅ shipped (placeholder quality; tuning deferred) | 2026-06-12 |
| 17 | UX Polish | ✅ | |
| 18 | Design Refresh | ✅ | |
| 19 | Bug Fixes | ✅ | |
| 20 | Device Management | ✅ | |
| 21 | BLE Persistence | ✅ | |
| 22 | Video Overlay Validation | 🚧 at device checkpoint | |
| 23 | Website | ✅ | 2026-06-11 |
| 24 | Parent Report Cards | ✅ | 2026-06-11 |
| 25 | Codebase Audit | ✅ | 2026-06-12 |
| 26 | In-App Video Overlay | 🚧 at EAS-build checkpoint | |
| 27 | Device Model (3D hero) | ✅ | 2026-06-12 |
| 28 | Privacy Policy | ✅ | 2026-06-13 |
| 29 | Marketing Content | ✅ | 2026-06-14 |
| 30 | Website Copy Polish | ✅ | 2026-06-15 |
| 31 | AI Coaching Chat | 📋 superseded by Phase 33 | |
| 32 | SoCal Coach Outreach Research | ✅ | 2026-06-16 |
| 33 | AI Coaching Chat v2 | ✅ | 2026-06-16 |
| 34 | Device Diagnostics | ✅ code (on-device verify deferred) | |
| 35 | Feature Verification & Doc Reconciliation | ✅ | 2026-06-18 |
| 36 | Coach-Friendly Metric Ratings | ✅ | 2026-06-17 |
| 37 | Team Coach Dashboard | ✅ | 2026-06-18 |
| 38 | Mobile UI/UX Redesign | ✅ | 2026-06-19 |
| 39 | Redesign Fixes & UX Iteration | ✅ | 2026-06-19 |
| 40 | Website Redesign (iOS match) | ✅ | 2026-06-22 |
| 41 | Race-Start Sequence (iOS) | ✅ | 2026-06-22 |
| 42 | Core-Flow Failsafes (iOS) | ✅ | 2026-06-22 |
| 43 | Demo Readiness (runbook) | 📋 doc, planning | |
| 44 | Encoder Data Integrity | 🚧 44-03 owed (warmup floor + overlay sync) | |
| 45 | Cloud Session Save (device_id UUID→TEXT) | ✅ | 2026-07-30 |
| 46 | Marketing Blog (build log) | ✅ | 2026-06-23 |
| 47 | Trial Annotation (review + ground truth) | ✅ | 2026-07-20 |
| 48 | Athlete-Create Fix | ✅ | 2026-07-30 |
| 49 | Security Hardening (backend) | 📋 planned | |
| 50 | Demo Team & Synthetic History | ✅ | |
| 51 | API Correctness & Audit | ✅ | |
| 52 | Sample-Rate Contract | ✅ | |
| 53 | Attention Allocation (SPC detection engine) | 📋 direction/planning | |
| 54 | Gate Removal (tier + stroke gating) | ✅ | |
| 55 | Athlete Flow Fixes (mobile) | ✅ | |
| 56 | Coach Chat Athlete Scoping | ⏸ open defect, unscheduled | |
| 57 | Annotation Workflow (annotate-tool v2) | ✅ | |
| 58 | Video Ground Truth (solo capture + annotate-from-video) | ✅ | |
| 59 | Segmenter Evaluation (ground-truth scoring harness) | ✅ | |
| 60 | Mobile App Rework (per-cycle analytics + video + chart windowing) | ✅ | |
| 61 | Web Portal Rework (report card + video + Compare) | ✅ | |
| 62 | Progress Report Rework | ⏸ unscheduled | |
| 63 | Data Flow Map (DATA-FLOW.md) | ✅ | 2026-08-13 |
| 64 | Fullscreen Video + Velocity Overlay (web) | ✅ | |
| 65 | Underwater Phase Detection (free / back / fly) | ✅ | |
| 66 | Acceleration Derivative (Savitzky–Golay) | ✅ | |
| 67 | External Camera Sync (GoPro / waterproof cam) | ✅ | |
| 68 | Persist Generated Session Names | ⏸ unscheduled | |
| 69 | Multi-Camera Video (up to 4 synced angles) | ✅ | |
| 70 | Video Session Matching | ✅ | |
| 71 | Video Surface Rework | ✅ | |
| 73 | Group Comparison | ✅ | |
| 74 | BLE Dump Reliability | 📋 planned (74-01 plan only) | |
| 75 | Report Card Revamp (Race-Phase Model: Start / Underwater / Swim) | 🚧 Start (75-04) + UW metrics + Step-3 UI (75-05 /phases) done; report-card CONSOLIDATION cutover shipped (75-07 `040ce0d`); **75-06 Swim+Whole metrics CLOSED 2026-08-28 — registry complete 46/47, 23 scalar specs, annotations-first per-cycle (43 trusted/44 provisional live), `PUT /annotations` phases-drop repair, library backfilled; ⚠ uncommitted (shares `api.py` with 82-01), AC-7 human-verify owed**; 75-08 compare-window → 75-09 unified trace still next | |
| 76 | Breakout Detection (free/back — kick-band disappearance) | ✅ | 2026-08-20 |
| 77 | Fly Breakout Detection (arm-cycle appearance) | ✅ | 2026-08-20 |
| 78 | Multi-Swimmer Segmentation Diagnostic (pure, no fixes) | ✅ complete (4/~15 swimmers annotated — coverage gap, not "one swimmer") | 2026-08-21 |
| 79 | Redefine dive_start_s (foot of first ≥X m/s surge) | ✅ complete (`e1934ba`; X=2.0, MAE 0.72→0.15 s; backfill applied) | 2026-08-21 |
| 80 | Stroke-Cycle Segmentation (count-centric re-measurement + tuning) | 🚧 CONTEXT done (freestyle-only; measure→re-tune wavelet) | |
| 81 | Annotation Video Marking (play-and-tap + on-video overlay + UW-kick marker) | 🚧 81-01 shipped (`a73db03`) — active camera = stage-fullscreen overlay w/ marker buttons (Dive/UW/Stroke/Finish + stroke-mark) + 4/8/All window presets in the bar; mark in fullscreen without exiting; keys 1/2/4/5+M retained; shared report components untouched. ⚠ human-verify owed. 81-02 (key-3 kick marks + backend recompute) owed | |
| 82 | Storage Quota Cleanup (session-delete storage leaks + Supabase Pro upgrade) | 🚧 planning — 82-01-PLAN.md created; free tier over quota (2.53 GB vs 1 GB); 716 MB orphaned from two leak sources in `DELETE /sessions/{id}`: `video_path` never removed, and `session_videos` cascade-deletes rows without removing their storage objects | |
| 83 | Per-Cycle / Per-Kick Trace Coloring (phase-section insets) | 🚧 **83-01 CLOSED 2026-08-28** (cycles half shipped + AC-7 approved — Swimming inset bands, ticks, amber outlier, annotated-vs-auto badge, hover readout, bidirectional highlight; new pure `web/lib/cycleBands.js`, reusable for kicks). **83-02 CLOSED 2026-08-28** (kicks half shipped, apply outcome approved — `metrics.segment_kick_bands` trough-to-trough with no new constant, `phases.kick_bands` persisted, schema 3→4, Underwater inset via 83-01's lib unmodified, breaststroke gated off, 63/81 non-breaststroke backfilled; **CONTEXT D5 REVERSED** — bands ride inside `phases`, not top-level `metrics_json.kicks`, so all three `PhaseContext` write sites get them from one change and they cannot go stale against an annotated window; peak dot removed everywhere at user direction, so AC-5's zero-diff on `PhaseVelocity.js` failed by the letter). **83-03 CLOSED 2026-08-29** (AC-8 approved on the SECOND attempt, first verify retracted) = breakout GOLD shipped as a synthetic `n: 0` band on annotated sessions only (the coach's streamline-break mark → their first stroke mark; measured positive on all 43 annotated, median 1.04 s). ⚠ **The plan's central feature — the k=3.0 MAD anomaly flag — was MEASURED AND CUT**: at a median 7 cycles a lap it fired on 75% of sessions at k=3.0 and still 39% at k=8.0, so no threshold separates clean from ragged. `web/lib/cycleShape.js` kept but PARKED and unwired; the fix needs a cross-session baseline (STATE item 17). **83-05 CLOSED 2026-08-29** (AC-8 approved after two live corrections) = cycle/kick OVERLAY panel — the replacement for the cut classifier: every cycle (Swimming) / kick (Underwater) laid on one shared axis beneath the inset, all-grey pack, left number gutter with hover-preview + click-to-pin, three-way cross-highlight, seconds axis with a normalized % toggle that adds a pointwise median; breakout excluded from the pack but shown as a `0 · breakout` gutter row. Frontend only, no schema, no backfill; suite still 497; the three protected components stayed byte-identical. Partially un-parks `cycleShape.js` (resample + median wired, MAD gate stays parked, item 17 stays open); ships the item-18 kick-tiling artifact knowingly. ⚠ **Two live corrections at the verify:** the gutter now WRAPS at 10 rows (a 15-dolphin-kick underwater outgrew the chart), and **AC-3 was OVERRIDDEN** — the breakout gutter row is no longer inert, it highlights the gold `n: 0` band on hover. ⚠ Two boundary widenings forced by contradictions inside the PLAN: `cycleShape.js` gained two `export` keywords, and `niceMax` is duplicated from the DO-NOT-CHANGE `PhaseVelocity.js` (guarded by a byte-equality check). ✅ New reusable **headless render-check harness** (`scratch/overlay_render_check.mjs`, 40 checks) that needs no auth — the concrete answer to 83-01's "build and lint are blind to this". Peak-alignment surfaced as a better axis mode and was DECLINED → STATE item 19. **83-04 owed** = inset window framing (context padding + minimum span), dropped out of 83-03, independent of 83-05. Phase stays 🚧 — plan/summary counts now match at **4/4**, the exact heuristic that has falsely signalled "done" at 83-01, 83-02 AND 83-03; **83-04 is scoped in STATE but still has no PLAN**. Original scope: alternating blue/purple bands per stroke cycle (Swimming inset) + per downkick (Underwater inset), grey outside, boundary ticks; new `metrics_json.kicks` persist; count badge w/ human-vs-auto provenance, per-band hover readout, bidirectional cross-highlight with CycleCharts, amber outlier outline. Stacks on uncommitted 75-06, shares its backfill | |
| 84 | Mobile App User Feedback (icon, upload failures, camera, orientation, indicators, brush gesture) | ✅ **COMPLETE 2026-08-31 — 5/5 plans, all loops closed, committed + pushed to `main` in BOTH repos.** ⏸ **The device-verify batch is OWED on one EAS build** (icon, portrait lock, GO marker + horn-off, indicator trace, brush drag, 720p upload + camera) — the phase's standing decision was that no item gets a human check until one build carries everything. ⚠ Backend deployed to Railway FIRST, so 84-02's load-bearing deploy ordering is now MET and the app build is safe to spend. CONTEXT done 2026-08-29 (`/paul:discuss`) — 6 user-reported items in the **separate `swimnetics-mobile` repo**; 4 root-caused during discuss, item 2 rescoped to a diagnostic. Native/JS split drives sequencing: **only 1 (AppIcon.appiconset, iOS-only per D1) and 4 (`Info.plist` `UISupportedInterfaceOrientations` still lists both landscape values — `app.json`'s `"orientation": "portrait"` is inert in a bare workflow) need a new EAS build**; 2/3/5/6 are JS. Item 2's premise was FALSE (both the video-file enqueue and the sync-origin save already auto-fire) → rescoped to "find out why uploads sometimes fail", with 6 code-grounded hypotheses, chief among them the 50 MB `MAX_VIDEO_BYTES` 413 against an unset `videoQuality`, the **Phase 82 over-quota bucket** (a possible hard prerequisite), and the **in-memory queue that dies on app restart with no error surfaced**. Item 5 is larger than reported — **three** indicator vocabularies, not two (dashboard 0–100 score excluding provisional / athlete-detail hardcoded `BAND_COLOR` ignoring both `rating_colors` and provisional / `PillarCards`' third inline derivation), so a provisional pillar is invisible, trusted, and warned-about depending on the screen; D3 makes band+`rating_colors` canonical across all three. Item 6 root cause = the brush `PanResponder` never sets `onPanResponderTerminationRequest`, which **defaults to `true`**, so the parent ScrollView steals the drag on any vertical drift. **Item 7 ADDED 2026-08-29 (second discuss round, D8–D13): the coach GO marker** — a silent button on both live recording states that stamps `Date.now()`, converted to session time against the existing `sessionStartPhoneMs` META correlation and sent as a new `POST /process` form field (not a follow-up `PUT` — item 2's silent-loss lesson). This closes STATE item 15 and fills `reaction_time`, which is 0/99 today. ⚠ It also **switches off the Phase-41 race-start sequence** (D9): the phone speaker is inaudible poolside, so the horn is a false affordance — and its blare fired *before* `writeCmd('START')`, which would have made an honest GO stamp negative and been rejected by the endpoint's `≥ 0` contract. ⚠ The metric carries the coach's own thumb latency, so it is a within-coach relative measure, not an absolute block reaction; no backfill is possible. Also corrected: 75-04's "phone↔encoder clock sync deferred" note is stale — the app has computed it since Phase 47. Item 5 is a **four**-surface problem, not three: `AthletesScreen.js:84` is a fourth indicator and the one that already does it right (`rc[band] \|\| BAND_FALLBACK[band]` reads the server's `rating_colors` first), so it is the pattern to standardize on. **84-01 PLAN created 2026-08-29** = the NATIVE-CONFIG lane (items 1 + 4) — chosen first because it touches **no JS at all**, so it cannot collide with the mobile repo's uncommitted Phase 74 work or with item 7's GO marker; 7 items are scoped as **5 plans**. ⚠ **G1: CONTEXT open question 2 answered NO — there is no OTA channel** (no `expo-updates`, no `updates` key, no eas channels), so **every** Phase-84 item ships in an EAS build and the native/JS split drives verification cost only, not sequencing. ⚠ **Three CONTEXT claims verified WRONG:** `sharp` is not in the mobile repo (it is in `web/node_modules`, so the plan rasterizes with Pillow in the backend repo); the icon "SVG" is a wrapper around a base64 **1004×960 RGBA PNG** bleeding to all four edges with the `<image>` anchored top-left in a 1028×1028 viewBox — no vector, alpha must be flattened (iOS rejects it; Phase 85's transparent web icons do NOT transfer), padding must be invented, and 1024 is a 2% upscale; and `AthleteDetailScreen` has **no access to `rating_colors`** (pillars arrive via `route.params.athlete`, not a fetch), so item 5 needs navigation-param plumbing, not a lookup swap. ✅ Items 4 and 6 root-caused exactly as CONTEXT said (2-line plist deletion, no JS orientation code, no `~ipad` key; neither `VelocityChart` PanResponder sets `onPanResponderTerminationRequest`), and `expo-file-system` is fine — a transitive dep of `expo`, present in the lockfile, so H6 stands. Plan carries a decision checkpoint (icon background + fill, judged at 120 px) and a build-gated human-verify (⚠ `expo-doctor` first). **84-02 PLAN created 2026-08-29** = item 7, the coach GO marker + race-start sequence off — cross-repo (`api.py` + `tests/test_api.py` here, `RecordScreen.js` + `startSequencePrefs.js` in the mobile repo), `depends_on: []` with zero file overlap with 84-01. **Closes STATE item 15**; `reaction_time` is 0/99 today. ⚠ **G6: the "phone↔encoder clock sync is deferred" claim is STALE** and has misled two documents (`api.py:1160` + 75-04 D13) — `RecordScreen.js:445-447` has computed it since Phase 47, so only the button was ever missing; both docstrings get corrected. ⚠ **G10: disabling the horn is not just a default flip** — `v === null ? true : v === '1'` means a flip misses everyone who explicitly opted in, so the plan bumps the SecureStore key to `startSequenceEnabled.v2`; the hook, overlay and audio assets stay byte-identical (D9 is "for now"). ⚠ Deliberate asymmetry with `PUT /go-signal`: that endpoint 422s on bad input, but on `/process` the request carries the **swim**, so a bad marker is dropped-and-logged and the session still returns 200 — pinned by an AC and a test because it is the thing a reader would "fix". ⚠ **Deploy ordering is load-bearing**: backend live on Railway BEFORE the app that sends the field, or sessions recorded in the gap lose the marker silently. ⚠ **No backfill is possible** — reaction_time fills only for sessions recorded after this ships, a permanent discontinuity `phaseBaseline.js` will see. ⚠ The metric embeds the coach's own thumb latency (within-coach relative, never absolute), so the human-verify must judge the NUMBER, not just its presence. Decision checkpoint owed on whether the now-pointless start-sequence toggle stays visible. **84-03 PLAN created 2026-08-30** = item 5, indicator formalization — mobile-repo only, `depends_on: []`, zero file overlap with 84-01/84-02 and the only lane touching none of `RecordScreen.js`/`BleContext.js`/`CycleCharts.js`, so it is clear of the uncommitted Phase 74 work. ⚠ **G20 demotes CONTEXT's diagnosis: `provisional` is STRUCTURALLY UNREACHABLE** — `ratings.py:206` needs a missing threshold, but `thr_table` falls back to breaststroke and all four pillar primary keys have rows, which `tests/test_ratings.py:142` confirms by asserting `is False`. So the reported "provisional pillar is invisible / trusted / warned-about depending on the screen" **cannot occur**; `PillarCards`' banner has never rendered and the dashboard's exclusion has never excluded anything. No device test exists for it — the harness proves it on synthetic input. ⚠ **G21: the band hexes are identical** between `ratings.py:33` and `tokens.js:40-42`, so `AthleteDetailScreen`'s hardcode is *latent drift*, not a visible bug. ✅ The real, observable defect (G22) is **four indicator FORMS for one band** — dot / lowercase word / 0-100 number / title-case word over a meter — plus (G23) `unknown`, which IS reachable and gets four different accidental answers. Plan = one shared `src/lib/indicators.js` + `BandDot.js` + a headless harness (G28: the mobile repo has no test runner or linter), decision checkpoint on the dashboard card, then four surfaces rewired; verifiable over Metro with **no EAS build**. One visible change: athlete-page labels go title case and `—` becomes "No data". New STATE item 21 asks whether the dead `provisional` flag should be re-armed, retired or left dormant — deliberately not decided here. **84-04 PLAN created 2026-08-30** = item 6, the brush-bar gesture — mobile-repo only, `depends_on: []`, **one component file** (`VelocityChart.js`) plus a new `scratch/gesture_check.mjs`; fourth of five, chosen next because it is the only remaining item touching neither `RecordScreen.js` (84-02's file + the uncommitted Phase 74 work) nor `CameraView`. ✅ **G31/G32: CONTEXT's root cause verified in RN 0.85.3's own source** (`PanResponder.js:520-522` defaults `onPanResponderTerminationRequest` to `true`; `:468-470` already defaults `onShouldBlockNativeResponder` to `true`, so the fix is one property per responder, not two). ⚠ **G33: the bug is WORSE on a surface CONTEXT never examined** — `RecordScreen.js:994` renders the results chart with `brush` and **no `onInteractionStart`/`End`** inside the `ScrollView` at `:919`, so it has no scroll lock at all; fixing in the component repairs it without touching 84-02's file. ✅ **G34: open question 6 answered NO twice over** — `VideoOverlayScreen` passes neither `interactive` nor `brush` (both responders are gated on exactly those props) and has no `ScrollView`. ⚠ **G35: "make the bar taller" is the obvious wrong fix** — a responder does not lose a gesture by leaving bounds; termination is the only loss path. ⚠ **G36: the fix's cost is the bug's mirror** — two thin dead zones where a drag starting on the 30 px strip can no longer scroll the page; deliberately **no decision checkpoint**, the tradeoff goes to the human-verify with the directional fallback pre-specified. ⚠ **G38: the guard is source-level** (AST via `web/node_modules/typescript`, with a mutant self-test) because `VelocityChart.js` imports `react-native` — it asserts configuration, never gesture behaviour. Verified over Metro; **no EAS build, no encoder**. **Remaining after this: items 2 + 3 as the fifth plan** — both touch `RecordScreen.js` and the same `CameraView` JSX, both need encoder-plus-camera to verify, and D5 already routes `videoQuality` into item 2's lane. ⚠ Two scoping facts for it: expo-camera 56.0.8's **iOS default is 1080p, not 4K**, so H1 is overstated; and there is **no pre-recording camera preview** (`onCameraReady` writes START and calls `recordAsync` at once), so a `facing` control has nowhere safe to live and `zoom` maps **exponentially** against a device max JS cannot read. **✅ 84-01 LOOP CLOSED 2026-08-30** (icon + portrait lock in the mobile tree, statically verified; EAS build deliberately deferred so one build carries the phase). **✅ 84-02 LOOP CLOSED 2026-08-30 — reconciled, not run:** its APPLY happened in an unrecorded session and was discovered by 84-01's closing `git status`, so UNIFY re-derived the evidence — full diff review, `pytest tests/` = **505 passed** (+8, baseline 497; STATE's older "485" is the stale 75-06 count), and a TypeScript parse of all three changed JS files (the mobile repo has no test runner) with a mutant self-test. **7 AC pass · 1 partial · 1 failed-by-decision · 3 clauses deferred to the device.** The owed decision checkpoint was answered **`hide`** in code (`SHOW_START_SEQUENCE_TOGGLE = false`), which knowingly costs AC-9's four-file blast radius (it is 5) and supersedes AC-7's "the toggle can still turn it back on" clause; a seed-state bug was fixed in passing (the async pref read let a fast Continue ship `startSequence: true` despite a stored OFF). One deliberate AC-3 exception is pinned by a test: `"banana"` **does** 422, at FastAPI's `Optional[float]` coercion above any handler code. 🔴 **Nothing is committed, pushed or built**, so `reaction_time` is still 0/99 and the load-bearing deploy ordering (backend live on Railway **before** the app that sends the field) is entirely unmet. 🔴 **84-03 and 84-04 are NOT APPLIED** — confirmed by direct inspection 2026-08-30 (no `indicators.js`/`BandDot.js`/`indicator_check.mjs`; no `onPanResponderTerminationRequest` in `VelocityChart.js`; no `gesture_check.mjs`), so neither can be unified. Phase 84 stands at **2 of 5 plans closed**; ⏸ **no Phase-84 item gets a human check until the phase is finished and one EAS build carries everything**. **84-05 PLAN created 2026-08-30** = the FIFTH and last plan, items 2 (upload failures) + 3 (camera options), grouped because D5 routes `videoQuality` into item 2's lane and that is the same `CameraView` JSX item 3 edits. 🔴 **Item 2's cause is MEASURED, not hypothesised** — a read-only probe was run live during planning. **H1 CONFIRMED and quantified:** 37 stored clips give a median encode rate of **1.38 MB/s** (0.90–2.20) at iOS's default 1080p, so the 50 MB cap bites at **≈36 s of video** (≈23 s on the fastest device) while `recordAsync` and auto-stop both allow 300 s. The bucket shows the ceiling: 97 phone clips, **max 48.3 MB, nothing ≥50 MB**. **9 sessions carry the lost-clip fingerprint** (`video_origin_s` set, `video_path` NULL); recovering each clip's length from `deviceDuration − origin` prices **6 at 54–110 MB**, two more inside the device spread — so **6–8 of 9 are H1**, one (25 MB) is the residual. ✅ **Open question 1 answered NO — H2 REFUTED, Phase 82 is NOT a prerequisite**: a 1-byte probe object uploaded successfully at 2.63 GB and was deleted, so storage is not rejecting writes (the long-standing *"new uploads may already be blocked"* worry is retracted). ⚠ **The 413 is invisible three times over**: the queue wraps it as `Server error (413)` and **retries it twice over ~13 s** on an outcome that cannot change; `UploadToast` renders a constant string and never shows `job.lastError`; and the chip dies with the app. ✅ **Open question 4 answered "not here"** — queue persistence (H3) stays out of scope: `saveVideoToLibrary` copies **every** recording to Photos (the footage is never lost, only the attachment) and the web annotate page can attach a clip to an existing session. **H4 is not a bug** (the early returns fire precisely when there is no `session_id`); **H6 is not implicated** — though checking it found that `expo-file-system` is **absent from `package.json` and not hoisted**, resolving only from `node_modules/expo/node_modules/`. ✅ **expo-camera's iOS source decides the design**: `zoom` touches only `device.lockForConfiguration()` so it is **safe mid-record**, while `facing` and `videoQuality` both reconfigure the session and must be set before recording — and with **no pre-recording preview**, the live overlay is the only place a coach can frame a shot. ⚠ **No zoom preset may claim a magnification factor** (`videoZoomFactor = maxZoomFactor ** zoom` against a device max JS cannot read); only `zoom=0` is truthfully 1×. ⚠ Two docs claims corrected: iOS defaults to **1080p, not 4K**, so H1's 4K framing was overstated (1080p alone does it), and the TS docstring calling the 16:9 `videoQuality` values Android-only is **wrong**. Plan = commit the probe as `tools/probe_video_uploads.py` → **decision checkpoint** on the quality lever (720p recommended / 1080p at 6 Mbps / both) → pure `uploadRetry.js` + pre-flight size refusal + no-retry-on-4xx + a chip that names the reason, guarded by a headless harness that cross-checks the constant against `api.py` → camera controls → **build-gated human-verify** (encoder **plus** camera). `api.py`'s `MAX_VIDEO_BYTES` deliberately untouched — Phase 82 / Pro territory, and it is a RAM guard as well as a quota one. Phase 84 closed at **5/5 plans written, 5/5 loops closed**. ⚠ The mobile commit necessarily swept in pre-existing uncommitted work that shares `RecordScreen.js` — Phase 74's retrieval hardening (stall 30 s→8 s, `MAX_RETRIEVAL_ATTEMPTS`, `sendDumpHandshake` retry, post-save `CLEAR`) and `BleContext`'s `requestMTU` — because the two are interleaved in adjacent hunks and no path-scoping separates them; said so in the commit message rather than faking a clean split. 🔴 **New: BLE DISCONNECT during dump reported 2026-08-31 → STATE item 24** (a different failure from Phase 74's stall; loop built at `tools/dump_stress.py`, not yet run) |2026-08-31 |
| 85 | Marketing Home Page Refresh (mark + race-phase repositioning + copy rewrite) | ✅ **COMPLETE 2026-08-29 (1/1 plans, `a75c373`, pushed to `main` so it is live).** CONTEXT done over **4 discuss rounds**; **85-01 APPLIED 2026-08-29, AC-8 approved on the live local site; loop CLOSED.** Shipped as planned with four deviations worth carrying: the copy check is scoped to the MARKETING surface, not all of `web/components` (the portal legitimately says "GoPro" and holds `changed (unclear)` in the `AlertSummary.js` that D27 forbids touching, plus ~285 comment dashes); the build now emits **20** static pages, not AC-7's 19 (measured: +2 icon routes, -1 favicon); the three phase cards align their radars with a per-card CSS **grid row** rather than the mockup's reserved `min-height`, which held at 1280 but broke at 880 where the underwater blurb wraps to a fifth line; and `Brand` uses `next/image` because `no-img-element` is enabled here. Marketing site untouched since `17086cb` (2026-06-22, Phase 40), so ten weeks of product work is invisible: race-phase report card, usual-range comparison, per-cycle bands, multi-camera video. Approved design = `scratch/website-home-mockup.html` (re-runnable build chain in `scratch/`). Page becomes hero → report card → usual range → cycle pack → video → device → how it works → quote; `Features.js` + `SampleChart.js` retired; the Swimnetics mark enters the site for the first time (nav + footer lockup + favicon). **Charts are drawn from ONE real coach-marked session** (Chantee "100%", butterfly, `85b18b3f`, all four boundaries `manual`) — trace geometry real and unmodified, every printable value deterministically perturbed, no athlete name, so **the page may never claim "real data"**. Baked to a static `web/lib/marketingGeom.js` at author time; no Supabase call on a public page. **D24 (alert semantics) drove a correction that matters beyond the site:** an alert fires ONLY outside the usual range, in-range is "Normal" and uncounted — ⚠ but the round-4 code read established the portal's `flagVerdict` **already implements exactly that**, so the earlier claim that the portal was wrong is RETRACTED; the only real gap is the out-of-range-ambiguous chip label ("changed (unclear)" → "to review"), deferred to a follow-up phase (D27, marketing-only scope). Also corrected: the em-dash inventory was wrong — the FAQ is **10 `&mdash;` entities to 2 literal characters**, so a character-only check reads clean while the page is not, and `app/layout.js` (2, inside `metadata`) was missed entirely. ⚠ `web/` auto-deploys to Vercel on push to `main` (R4), and the plan removes the FAQ's stroke-validation answer (D6/R3) and publishes a breakaway-magnet safety claim (D25/D29, user-confirmed real) |2026-08-29 |
| 86 | Session Clock Accuracy (absolute, measured session start) | 🚧 **86-01 LOOP CLOSED (PLAN ✓ → APPLY ✓ → UNIFY ✓) 2026-08-31 — 1 of 3 plans; phase NOT transitioned, no phase commit.** Suite **505 → 520** (+15, exactly the new-test count, so nothing pre-existing was altered). `GET /time` live and provably I/O-free (a test monkeypatches every Supabase entry point to raise); three optional form fields on `POST /process` validated and persisted; `patch_14` written and **NOT applied**. ⚠ **AC-1/AC-5 proven only against the MOCKED insert** — nothing has touched a real column. ✅ **The Phase-84 gate was LIFTED, verified not assumed** (both repos clean at `origin/main`; deployed `/openapi.json` returns 200 containing `go_signal_s`). ✅ **A second ordering hazard was found during APPLY and designed out:** the plan's Task 2 said to add the three keys to the `session_row` **literal**, which would have made deploying `api.py` before the user applies patch_14 break **every upload** (PostgREST rejects unknown columns) and would have failed `TestSchemaContract`, whose static extractor checks every column named in an `.insert({…})` literal against `live_schema.json`. Avoided via the Phase 70 `recording_token` precedent — conditional **subscript** assignment — so **patch_14 and the deploy can land in either order.** Un-planned additions: `_finite_or_none()` (a NaN/inf diagnostic serializes to invalid JSON and would fail the whole insert, violating AC-3's own principle) and a 4th bad-value case (micros-for-ms). Sanity window is 2020-01-01 → now+48h, wide on purpose — both realistic unit errors land nowhere near a real "now". **86-02 must not send seconds.** ⚠ **86-01 is uncommitted**, so it is not on Railway; deploy ordering stays load-bearing (→ STATE items 26–27). 86-02/86-03 not yet written. New phase out of the SwimClips integration analysis, but plans 01–02 stand alone — they fix a live defect in the shipped video overlay. Every sample is stamped `micros()` (boot-relative, wraps 71.6 min); absolute time exists only as `sessionStartPhoneMs`, computed on the phone from META (`RecordScreen.js:457`), used for the local overlay and **thrown away** — never sent to the server. `api.py` never sets `recorded_at`, so the DB default `NOW()` stores **upload time, not swim time** (→ new STATE item 22, deliberately NOT fixed here: it is a library-wide comparability break). So nothing can answer *"what UTC instant was sample #0?"*, and the phone's mapping carries an **unmeasured 20–80 ms error** — the BLE flight time between `sendMeta()` sampling `micros()` and iOS firing the callback. ✅ **Firmware OUT of scope for the whole phase, verified not assumed:** `sendMeta()` already returns `device_now_us` captured at send time (`ESP_32_V5.ino:426`) and `pendingMeta` is a plain re-arming flag (`:528`), so **META is idempotent and repeatable** — the round trip runs against the existing command and the only blocker is the phone-side `metaSeenRef` one-shot (`RecordScreen.js:440`). No flash, no device access, no risk to buffer-and-dump. ⚠ **`GET /time` must be UNAUTHENTICATED as a correctness requirement:** `require_auth` (`api.py:74`) calls `sb.auth.get_user(token)`, a **network round trip to Supabase per request**, and the client uses this endpoint to measure its own RTT and derive clock offset from RTT/2 — an uncontrolled network call inside the handler lands *inside the interval being measured*. Reads like an auth oversight, so the reason is pinned in a comment and an AC. ⚠ **GATED:** `api.py` holds **uncommitted 84-02** work in the same `process_session` signature 86-01 edits, `tests/test_api.py` holds its `_post_csv` helper, and the mobile repo has 14 modified files incl. `RecordScreen.js` (84-02's file, 84-05 planned against it) — prerequisite is Phase 84 committed and its backend deployed. ⚠ **Deploy ordering load-bearing** (84-02's constraint, now doubled): backend live on Railway **before** the 86-02 build, or FastAPI drops the unknown form fields silently and sessions in the gap lose their start with no error. ⚠ **No backfill possible** — only the phone produces this at record time, so all 99 existing sessions hold NULL permanently; consumers must treat NULL as *unknown* and never substitute `recorded_at`, same reasoning that forbids backfilling NULL `sample_rate_hz` with 100. Decisions: storage = **new columns, BIGINT epoch ms** (`patch_14`, unapplied) not `metrics_json.phases` — session provenance like `sample_rate_hz` (patch_09), and `phases` is rewritten by recompute and `PUT /annotations`. **3 plans: 86-01 backend (✅ loop closed 2026-08-31, suite 505→520, uncommitted) · 86-02 mobile round-trip + send (📋 PLAN created 2026-08-31) · 86-03 tap test (not written).** **86-02** replaces the one-shot META with a **burst of 10 timed round trips**, keeps the **minimum** RTT (Cristian's) and corrects `sessionStartPhoneMs` by `minRTT/2` — the live overlay fix, since `deviceNowUs` is captured when the ESP32 *builds* the reply, so the inbound leg is currently attributed to the encoder and the start is biased **late**. Measures the phone’s offset against `GET /time` **concurrently with the ~20 s dump** (zero added wall clock) and sends all three fields on the swim’s own `POST /process`. New pure `sessionClock.js` (zero imports) + `RecordScreen.js` + `scratch/session_clock_check.mjs` (84-05 harness pattern — the mobile tree has no test runner). 🔴 **`Math.round` is load-bearing:** `api.py` declares `Optional[int]`, so a fractional string 422s at **Pydantic coercion, before the handler runs**, and 86-01’s drop-don’t-422 posture cannot protect a value that never reaches it — the whole swim is lost. ⚠ 86-01’s **D2 window** (2020-01-01 floor → `now + 48 h`) means seconds-for-ms is discarded with a log the phone never sees, so the harness parses the two constants out of `api.py` and fails on drift. ✅ `clock_offset_ms` is **measured and reported, never applied** — the app records at poolside where `GET /time` can fail outright, so applying it would make the primary number depend on an unrelated network call; sign documented as **positive = phone ahead of server**. ⚠ The probe may never cost a swim (AC-2), and must not race the Phase 74 stalled retry (generation ref). ⚠ 84-02’s GO marker now resolves against the corrected start (~`rtt/2` shift, far below the coach thumb latency it already embeds). ⚠ `rtt/2` stays an **ESTIMATE until 86-03**; bounded by two verified facts — `processPending()` is the first call in a free-running `loop()` with no `delay` while not recording, and both BLE directions are acknowledged. ⚠ AC-7 is **build-gated** and rides Phase 84’s owed EAS batch ⚠ **Every accuracy figure in this phase is an ESTIMATE until 86-03 runs** — hence its own plan; it needs an EAS build and rides the same one as Phase 84's owed human-verifies | |
| 87 | Stroke-Level View + Arm Asymmetry (freestyle / backstroke) | ✅ **COMPLETE — 2 of 2 plans closed 2026-08-31** (01 backend applied + backfilled; 02 frontend toggle + visuals applied, human-verify approved). ✅ **87-02 shipped:** a **cycles / strokes** toggle in the Swimming section header that switches four surfaces together (inset bands, count badge, `CycleOverlay` pack, all four `CycleCharts` panels), a per-side-tinted pack with two per-side medians, stroke-level means re-derived client-side (new pure `web/lib/strokeStats.js`), and an **Arm balance** block rendering the three signed asymmetry percentages + four per-side CVs. Side A is blue and side B purple in the inset, the pack and the chips at once — `PhaseVelocity`'s `s.n % 2` band colouring already agreed with 87-01's even-array-position side A, so the band renderer did not change. 9 files; new `scratch/stroke_toggle_check.mjs` **63/63**, `overlay_render_check.mjs` 40/40 and `marketing_render_check.mjs` 45/45 both **unedited**, `npm run build` clean, `pytest` 563 green (no Python in the diff). ⚠ **Three deviations, all stated in the SUMMARY:** AC-7's hover/pin clear sits in the toggle's click handler not an effect (the repo's eslint errors on `react-hooks/set-state-in-effect`); `npx eslint .` is **not clean at baseline** (25/22 pre-existing) and the diff adds exactly one new error, the D2-mandated localStorage hydrate; and the render check cannot reach stroke mode from outside (`renderToStaticMarkup` runs no effects), so it rewrites the one state initializer in its own transpiled copy and the hydrate itself is covered only by the human-verify's reload step. ⚠ **The phase closes at 2/2 with two things deliberately left OPEN, not resolved** — STATE items 10 and 28: backstroke is still unverifiable (0 annotated sessions, and the asymmetry now renders for it too), and the auto-path asymmetry is still uncorrelated with coach-mark truth (r = −0.06) but is now a sentence a coach reads. ⚠ Deploy ordering (STATE item 29): backend before frontend, phase committed locally and **not pushed**. ✅ **87-01 shipped:** `metrics.segment_strokes`, `_derive_item_metrics` (the per-cycle loop extracted verbatim and shared), `_arm_asymmetry`, `annotations` → `stroke_bounds`, both api.py write sites, and `tools/backfill_strokes.py` — suite 520 → **563 green**, no pre-existing test modified. Backfill applied by the user: **47 of 101 sessions written** (24 from coach marks, 20 auto, 3 with a window but no strokes, 11 asymmetry left None for <3 strokes per side, 54 skipped non-free/back, 0 failed). ⚠ **One plan deviation:** `segment_strokes` gates on stroke_type, NOT on the segmenter's `.k` — fly/breast carry `k=2` (a detector property), so the planned `.k` gate would have contradicted the plan's own AC-1. ⚠ 3 freestyle sessions have a resolvable window but **zero strokes** (pre-existing — they had no cycles either — but newly visible); 87-02 will render an empty stroke view for them. Makes the individual **arm stroke** a stored unit: new `metrics_json.strokes` beside `cycles`, plus 7 session keys (3 signed asymmetry percentages — tempo / distance / peak velocity — and 4 per-side CVs). ✅ **Four read-only probes run against the live library during planning**, applying 83-03's lesson preemptively. **The signal is real and it separates:** across 23 usable annotated freestyle sessions the odd/even contrast is **6.1% median in tempo** (0.4–29.4%) and **9.4% in distance per stroke** (0.1–33.2%), clearing the within-side noise floor (t>2) on 7/23 and 9/23; some sessions sit at 0.4%, others at 30%. 🔴 **On AUTO sessions the same number is uncorrelated with truth — Pearson r = −0.06**, median error **10.2 percentage points** against a 6.1% signal, agreeing on only **2 of the 7** most-lopsided sessions. Cause is **parity, not precision**: un-paired wavelet boundaries land at **1.10× the coach's mark count** (0.50–1.33) and match 88% of marks within 0.35 s, but one extra or missing boundary **flips the A/B assignment of every later stroke**. ⚠ **The user saw this and chose to ship the number on auto sessions anyway** (D2), marked only by the existing `auto` chip — their call, recorded not softened. ⚠ **Backstroke has 0 annotated sessions** (census: freestyle 45/24 annotated, backstroke **2/0**, butterfly 29/16, breaststroke 20/4, udk 5/1); it shares freestyle's code path exactly so inclusion is free, but nothing about it is verifiable — confirms STATE item 10. ⚠ **The user's storage premise was wrong and was corrected before planning: stroke segmentation is NOT stored today** — `metrics_json.cycles` is already the *paired* product, coach marks live in `session_annotations.stroke_marks_s` (a table the report card never queries), and the auto boundaries are built then **discarded inside `_pair_boundaries`** (`metrics.py:388-397`), so the backend must start *emitting* strokes. Decisions: D1 **top-level `strokes` beside `cycles`**, not inside `phases` — 83-02's D5 reversal does not transfer, since `strokes` comes from the same call and the same two write sites as `cycles` and so cannot go stale against it; no `SCHEMA_VERSION` bump (that version lives inside `phases`). D3 sides are **A / B, never left/right** — a 1-D axial encoder cannot observe which arm is which. D4 the Swimming **usual-range strips do NOT switch** with the toggle (baselines are five *cycle-level* swims deep), so `phase_metrics.py` is untouched and both `needs_cycles` specs are out of scope. D5 backfill is a **new additive-only tool** writing exactly two keys, user-run at a blocking checkpoint. ⚠ Blast radius measured small — only **2 of 47** registry specs read `ctx.cycles`. ⚠ The riskiest edit is a **refactor, not a feature**: the per-cycle derived-metrics loop is extracted verbatim into `_derive_item_metrics` so cycles and strokes cannot drift, and AC-2 (cycles byte-identical, 505 suite green, no pre-existing test modified) is what proves it. ⚠ The `_anchors_from_marks` **leading-pad drop must be SHARED, not reimplemented** — 59-05 measured boundary F1 **0.000 with the pad vs 0.458 without**, invisible to `stroke_rate_spm`. **87-02 PLAN created 2026-08-31** = the frontend half — `depends_on: ["87-01"]`, wave 2, 4 auto tasks + a blocking human-verify, 9 files, web portal only. A **cycles / strokes** toggle in the Swimming section header switches four surfaces together (inset bands, count badge, `CycleOverlay` pack, all four `CycleCharts` panels) from `metrics_json.cycles` to `metrics_json.strokes`, plus a new **Arm balance** block rendering 87-01's three signed asymmetry percentages and four per-side CVs. ✅ **Two live code findings, both load-bearing:** (1) **the A/B sides are already drawn for free** — `PhaseVelocity.js:236-241` has painted bands `s.n % 2 ? var(--color-cycle-a) : var(--color-cycle-b)` since 83-01, and 87-01 D3 makes side A the even array positions, so `stroke_num` 0 → `n = 1` → odd → `cycle-a`: **A is blue, B is purple, with zero change to the band renderer** (the tokens were named `cycle-a`/`cycle-b` before there were sides). ⚠ It is an accidental alignment of two independent conventions that holds only while `stroke_num` is dense and 0-based, so AC-4 and a render assertion pin it rather than a comment. (2) **the whole `PhaseReportCard` renders headlessly** (`scratch/_prc_render_probe.mjs`, 55 KB of markup with badge, overlay and all four chart captions), so the toggle is machine-checkable end to end — ⚠ but **recharts emits an empty wrapper under `renderToStaticMarkup`**, so the dashed mean line and the dots are NOT assertable and the harness keys on the captions carrying the same numbers. **Decisions:** D2 toggle state local to `PhaseReportCard`, **not** lifted into `useTracePrefs` (that hook is shared with `/video`, where granularity is meaningless), persisted globally at `swimnetics.swimGranularity`, hydrated in an effect never a lazy initializer. D3 effective mode is `strokes?.length ? pref : "cycle"` and the fallback is never persisted. D4 stroke-level means are **re-derived client-side** (new pure `web/lib/strokeStats.js`, population std to match numpy) — 87-01 stores no stroke means and a cycle is two strokes, so the stored `mean_isi_s` would draw the dashed line clean off the top of the dots. D6 **one deliberate exception to `PhaseVelocity.js`'s standing DO-NOT-CHANGE**: an optional `itemLabel` used ONLY in its aria-label, which would otherwise read "one band per cycle" to exactly the users who cannot see the colours — geometry untouched, diff bounded by AC-8. D7 the overlay pack is **tinted by side** in stroke mode and its single median replaced by **two per-side medians** (normalized only, `MIN_ITEMS = 5` per side); 83-05's all-grey rule was about not *asserting* which stroke is odd, and naming which arm produced a trace asserts nothing. D8 **no warning banner on auto sessions** — 87-01 D2 is the user's explicit call; what is added is the *definition* of A and B, without which the readout is unreadable. D9 the readout gives **magnitude and direction, never a verdict** — no "even" threshold (83-03's cut classifier is the precedent), no good/bad colour, and the whole block is **unit-invariant**. D10 one 8-word line in stroke mode, *"Usual-range comparisons below stay per cycle"*, because 87-01 D4 keeps the strips cycle-level. ⚠ **GATED on 87-01 being applied** incl. its backfill checkpoint — the harness is synthetic and runs without it, the human-verify does not. ⚠ `scratch/overlay_render_check.mjs` (40 checks) and `scratch/marketing_render_check.mjs` must pass **unedited**. ⚠ Closing this plan puts the phase at 2/2, the same heuristic that falsely signalled "done" at 83-01/02/03 — backstroke stays unverifiable (0 annotated) and the auto-path asymmetry stays uncorrelated with truth | |

_Phase 72 was never used. 56 / 62 / 68 are unscheduled TODOs with no phase directory — live tracking is in [STATE.md](STATE.md)._
