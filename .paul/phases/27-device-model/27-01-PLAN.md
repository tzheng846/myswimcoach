---
phase: 27-device-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/public/models/device.glb (new, moved from repo root), web/components/three/DeviceScene.js, web/public/models/README.md, device_model.glb (deleted from repo root)]
autonomous: false
---

<objective>
## Goal
Replace the primitive placeholder device in the marketing hero with the real
`device_model.glb`, auto-fitted (centered + scaled) so it sits correctly in frame,
keeping the existing auto-rotate + pointer-parallax interactivity intact.

## Purpose
The hero is the marketing front door (PROJECT.md — website). The 3D scene was
explicitly built in Phase 23-01 as a placeholder with a "drop the GLB in later"
seam; this is that drop-in, made robust to the arbitrary scale/orientation a
Fusion 360 export carries.

## Output
- `web/public/models/device.glb` (the real model, serving in place of placeholder)
- `DeviceScene.js` updated to auto-fit the loaded model
- Repo-root `device_model.glb` removed (redundant once placed)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/23-website/23-01-SUMMARY.md — 3D hero design: GLB drop-in seam,
  ModelBoundary→PlaceholderDevice fallback, Rig idle-rotation + parallax + reduced-motion

## Source Files
@web/components/three/DeviceScene.js — the only component to change. Current state:
  GLB_PATH="/models/device.glb" (already the target path), GLBDevice renders the raw
  scene with NO centering/scaling; Rig wraps it in `group position={[0,-0.25,0]} scale={1.05}`.
@web/public/models/README.md — drop-in instructions (now being fulfilled)

## Decisions locked at planning (user)
- Interactivity: KEEP auto-rotate + cursor-tilt parallax (no OrbitControls, no drag).
- File size: SHIP AS-IS (8.24 MB) — no Draco/meshopt/gltf-transform tooling, no new deps.
- Placeholder stays as the error-boundary fallback (zero downside).

## Known facts
- `device_model.glb` is 8.24 MB at the REPO ROOT (untracked). Next.js only serves
  files under `web/public/`, so it must physically live at `web/public/models/device.glb`.
- Fusion 360 GLB exports commonly use +Z-up (Three.js is +Y-up) and an arbitrary
  origin/scale — so the model will almost certainly need recentering + auto-scaling,
  and possibly a one-axis rotation. Exact orientation is unknowable until rendered →
  resolved at the checkpoint, not guessed now.
- PBR/metallic materials can render near-black without image-based lighting; the scene
  currently has only analytic lights (ambient + 2 directional + point).
</context>

<acceptance_criteria>

## AC-1: Real model replaces placeholder, correctly framed
```gherkin
Given web/public/models/device.glb exists (the real export)
When the marketing hero loads
Then the real device model renders (not the primitive placeholder)
And it is centered in the canvas and scaled to a similar footprint as the old
  placeholder (largest dimension ≈ 2.5–3 scene units), not clipped, tiny, or off-frame
And no console errors and no SSR/hydration warnings
```

## AC-2: Interactivity preserved
```gherkin
Given the model is rendered
When the page is idle
Then the model slowly auto-rotates about its vertical axis
And when the pointer moves over the hero, the model tilts subtly toward it (parallax)
And when prefers-reduced-motion is set, the idle auto-rotation is frozen
```

## AC-3: Robustness + build
```gherkin
Given the model file is present
When `npm run build` runs
Then it exits 0
And if device.glb were ever removed, the hero still falls back to the placeholder
  (ModelBoundary/Suspense path intact)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Place GLB + auto-fit loader</name>
  <files>web/public/models/device.glb, web/components/three/DeviceScene.js, web/public/models/README.md, device_model.glb (delete)</files>
  <action>
    1. Move the file: repo-root `device_model.glb` → `web/public/models/device.glb`
       (the path GLB_PATH already points at). Remove the repo-root copy after the move.
    2. Rewrite GLBDevice in DeviceScene.js to AUTO-FIT the loaded scene so it does not
       depend on the export's scale/origin:
       - useGLTF('/models/device.glb'); work on the loaded scene (clone if needed to
         avoid mutating the cached gltf across reloads).
       - Compute bounding box (new THREE.Box3().setFromObject(scene)); get center + size.
       - Recenter: translate so the bbox center sits at the origin.
       - Auto-scale: uniform scale = TARGET / maxDimension, TARGET ≈ 2.6 (matches the
         placeholder's on-screen footprint so the existing camera/Rig framing still works).
       - Wrap in a group exposing an ORIENTATION offset (e.g. rotation prop, default
         [0,0,0]) so the checkpoint can correct Fusion's up-axis (likely rotation-x
         -Math.PI/2 if it exported Z-up) WITHOUT touching the Rig's animated rotation.
       - useGLTF.preload('/models/device.glb').
       Keep import three for Box3/Vector3.
    3. Do NOT change the Rig (idle rotation + parallax + reduced-motion) or the
       ModelBoundary/Suspense fallback — auto-fit happens INSIDE GLBDevice, the Rig keeps
       animating whatever it wraps.
    4. Lighting contingency: render first. IF the model appears near-black/flat (PBR
       metallic surfaces need image-based lighting), add drei <Environment preset="city" />
       inside the Canvas (note: fetches an HDRI from the drei CDN at runtime — acceptable,
       no local asset/build step). Only add it if needed — don't add preemptively.
    5. Update web/public/models/README.md: note the model is now present; keep the
       Fusion-export guidance for future replacements.
    Avoid: OrbitControls/drag (user chose hands-off), any compression/gltf-transform
    tooling or new deps (user chose ship-as-is), hardcoding a scale that assumes the
    export's units, mutating the shared useGLTF cache (causes drift across HMR reloads).
  </action>
  <verify>`npm run build` exits 0; preview hero shows the real model centered + auto-rotating with no console errors; temporarily renaming device.glb falls back to the placeholder</verify>
  <done>AC-1, AC-2, AC-3 satisfied (orientation fine-tune may remain for the checkpoint)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real device_model.glb in the marketing hero, auto-centered/scaled, with the existing auto-rotate + cursor-tilt interactivity</what-built>
  <how-to-verify>
    1. Run: cd web; npm run dev → open http://localhost:3000
    2. Hero: confirm the REAL device renders (not the primitive placeholder), centered
       and reasonably sized (not tiny/giant/clipped)
    3. Orientation: is the device's "up" actually up and a sensible face toward the
       camera? If it's lying on its side / upside down, say so — I'll apply the
       corrective axis rotation (Fusion Z-up → Three Y-up is the usual culprit)
    4. Motion: confirm slow idle spin + subtle tilt toward the cursor; lighting looks
       good (not black/washed out)
    5. Resize narrow (~390px): model stays in frame, no horizontal overflow
  </how-to-verify>
  <resume-signal>Type "approved", or describe what's off (orientation / scale / lighting) and I'll adjust then re-verify</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any web page/component other than DeviceScene.js + the models/ assets
- Rig interactivity contract (auto-rotate, parallax, reduced-motion) — preserve as-is
- PlaceholderDevice.js (kept as fallback)
- api.py, metrics.py, vel_acc_extraction.py, iOS repo, firmware

## SCOPE LIMITS
- No OrbitControls / drag-to-rotate (explicit user decision)
- No GLB compression or build-time asset tooling (explicit user decision)
- No new npm dependencies (drei <Environment> is already available via @react-three/drei)
- No changes to Hero layout, camera framing constants beyond what auto-fit requires

</boundaries>

<verification>
- [ ] `npm run build` exits 0
- [ ] Hero renders the real GLB, centered + auto-rotating, no console errors
- [ ] Reduced-motion freezes idle rotation
- [ ] Fallback-to-placeholder path still intact
- [ ] Checkpoint approved (orientation/scale/lighting confirmed by eye)
</verification>

<success_criteria>
- The marketing hero shows the real Swimnetics device with intact, elegant interactivity
- Repo-root device_model.glb removed; single source at web/public/models/device.glb
</success_criteria>

<output>
After completion, create `.paul/phases/27-device-model/27-01-SUMMARY.md`
</output>
