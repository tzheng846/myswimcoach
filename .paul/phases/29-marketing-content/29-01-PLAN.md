---
phase: 29-marketing-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/faq/page.js
  - web/components/Nav.js
  - web/components/Footer.js
  - web/components/marketing/Pricing.js
  - web/app/privacy/page.js
  - marketing/sales-pitch-email.md
  - .paul/PROJECT.md
autonomous: false
---

<objective>
## Goal
Publish a website FAQ page and produce a ready-to-send sales-pitch email, both
reflecting the new pricing model — and bring the rest of the marketing site into
consistency with that model.

## Purpose
The FAQ converts the real coach objections surfaced in the 2026-06-14 roleplay
(stopwatch sufficiency, "I don't have time to learn data," pool-time/throughput,
durability, "does it replace me") into public, scannable answers. The email gives
the user a direct outreach asset. Both must agree with the live site — which today
shows $15/swimmer/month and a "no video is ever stored" privacy promise, BOTH of
which the new pricing decision supersedes.

## Output
- New `/faq` marketing page (dark theme, matches `privacy/page.js` pattern)
- FAQ linked from Nav + Footer
- `Pricing.js` updated: $300 one-time device (basic metrics) + $20/swimmer/month
  cloud tier (video storage, long-term tracking, history, reports)
- `privacy/page.js` updated so cloud video storage is accurately disclosed
- `marketing/sales-pitch-email.md` — copy-paste cold-outreach email
- PROJECT.md pricing decision note updated
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files (patterns to match)
@web/app/privacy/page.js          # content-page pattern: Nav + Section + Footer, dark tokens
@web/components/marketing/Pricing.js
@web/components/Nav.js
@web/components/Footer.js

## New pricing model (2026-06-14 user decision — supersedes Phase 23 $15/swimmer/mo)
- $300 one-time device purchase → basic stroke-level metrics
- $20 / swimmer / month cloud tier → video storage, long-term progress tracking,
  session history, parent reports
- CTA stays "early access — reach out" (match current site posture, no trial promise)

## Privacy-policy note
The live policy (Phase 28) states no video is uploaded/stored. The new cloud tier
stores video. Policy MUST be updated to stay truthful. Storing MINORS' video raises
the legal stakes (COPPA) — see memory legal_privacy_status; flag, do not block.
</context>

<acceptance_criteria>

## AC-1: FAQ page live and navigable
```gherkin
Given the marketing site
When a visitor opens /faq (or clicks the FAQ link in Nav or Footer)
Then they see a dark-themed FAQ page matching the site, answering the core coach
  questions (value vs stopwatch, ease of use, coach-not-replaced, pool-time/throughput,
  durability, supported strokes, pricing, data safety) with no console errors
```

## AC-2: Site pricing is internally consistent
```gherkin
Given the FAQ states $300 device + $20/swimmer/month cloud
When a visitor also views the homepage Pricing section
Then Pricing.js shows the SAME model (no lingering "$15/swimmer/month")
```

## AC-3: Privacy policy matches the offered service
```gherkin
Given the cloud tier stores video
When a visitor reads the Privacy Policy
Then it no longer claims video is never uploaded/stored; it discloses optional
  cloud video storage and lists it under information collected / service providers
```

## AC-4: Sales-pitch email exists as a usable artifact
```gherkin
Given the user wants to email coaches
When they open marketing/sales-pitch-email.md
Then there is a subject line + body (problem, solution, what it measures, pricing,
  early-access CTA) consistent with the FAQ and the site, ready to copy-paste
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: FAQ page + Nav/Footer links</name>
  <files>web/app/faq/page.js, web/components/Nav.js, web/components/Footer.js</files>
  <action>
    Create web/app/faq/page.js mirroring privacy/page.js structure: import Nav +
    Footer, export `metadata` (title "FAQ — Swimnetics" + description), reuse the
    same `Section` helper pattern and dark tokens (text-ink / text-subtle / text-muted /
    text-primary, max-w-3xl container, py-16). Use a Q/H2 + A pattern.

    FAQ questions (derive answers from the roleplay; honest, coach-voice, no hype):
    1. What does Swimnetics measure that a stopwatch doesn't? — stroke rate, fatigue
       index, stroke-to-stroke consistency, full velocity profile, distance-per-stroke;
       the "why behind the time," two identical splits can be very different swims.
    2. Do I need to understand data analysis to use it? — No. Plain-English, visual
       results on your phone; you read it like a split.
    3. Does this replace me as a coach? — No. Second set of eyes; you make the call,
       video overlay lets you verify what the number means.
    4. How does it fit into practice without wasting pool time? — Dedicate one lane to
       testing, rotate swimmers through 3–4 trials; frequency is your call; add a second
       device for more throughput.
    5. How durable is it? — PETG casing (chemical/UV/impact resistant), splashproof with
       sealants; UHMWPE tether ~15× stronger than steel with a breakaway mechanism;
       ships with a spare tether.
    6. Which strokes are supported? — Breaststroke is fully validated today; freestyle and
       other strokes are supported at early/placeholder quality and improving. Be honest
       (matches segmentation_reliable=False in the pipeline) — do NOT claim full accuracy.
    7. How much does it cost? — $300 one-time for the device (basic stroke metrics);
       optional $20/swimmer/month cloud tier adds video storage, long-term progress
       tracking, session history, and parent reports. One shared device covers a lane.
    8. Is my swimmers' data safe — especially minors? — Brief answer + link to /privacy;
       club-as-customer consent model; data not sold or used for ads.

    End with a CTA block linking mailto:hello@swimnetics.com (early-access wording, match
    Pricing.js) and a "← Back to home" link like privacy page.

    Then add an FAQ link to:
    - web/components/Nav.js `links` array: { href: "/faq", label: "FAQ" }
    - web/components/Footer.js: a Link to /faq next to the Privacy Policy link, same styling.

    Avoid: inventing metrics the pipeline doesn't produce; promising freestyle accuracy;
    promising a free trial (CTA is early-access reach-out only).
  </action>
  <verify>npm run build (in web/) compiles; /faq renders in preview with no console errors; Nav + Footer show FAQ link</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Pricing + Privacy consistency updates</name>
  <files>web/components/marketing/Pricing.js, web/app/privacy/page.js, .paul/PROJECT.md</files>
  <action>
    Pricing.js — replace the single $15/swimmer/month card with the new two-part model:
    a $300 one-time device line (basic stroke metrics) and a $20/swimmer/month cloud tier
    (video storage, long-term tracking, session history, parent reports). Keep the existing
    section layout, tokens, check-list style, and the "Get early access" mailto CTA. Update
    the `included` list so it matches what each part actually provides (move cloud-only
    items under the cloud tier).

    privacy/page.js — update for accuracy now that the cloud tier stores video:
    - §2 "Information we collect": remove/replace the "We do not collect video" paragraph;
      add a bullet that video recorded in-app is stored ONLY when the coach uses the
      optional cloud tier, and stays on-device otherwise.
    - §4 "Service providers": if video is stored via Supabase storage, ensure the Supabase
      bullet covers video files (extend wording, no new provider unless one is actually used).
    - §6 "Children's privacy": add a sentence that video may include minors and is handled
      under the same club-consent model. (Flag to user at checkpoint — may want attorney
      review per legal_privacy_status memory.)
    - Bump LAST_UPDATED to "June 14, 2026".

    PROJECT.md — update the Phase 23 pricing line in "Should Have" to note the 2026-06-14
    supersession: "$300 device + $20/swimmer/month cloud (video storage, long-term tracking)"
    replaces the $15/swimmer/month presentation. One-line decision note; do not rewrite the section.

    Avoid: touching unrelated privacy sections; changing the Supabase project or providers;
    altering Pricing.js section IDs/anchors (the Nav #pricing link depends on id="pricing").
  </action>
  <verify>npm run build compiles; homepage Pricing shows $300 + $20 model and zero "$15"; /privacy shows updated video disclosure + new date</verify>
  <done>AC-2 + AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Sales-pitch email artifact</name>
  <files>marketing/sales-pitch-email.md</files>
  <action>
    Create marketing/sales-pitch-email.md — a copy-paste cold-outreach email to a swim
    coach / academy. Structure:
    - Subject line (e.g. "A stopwatch tells you how fast — Swimnetics tells you why")
    - Short greeting + 1-line hook (the two-identical-splits insight)
    - Problem (eye + stopwatch can't quantify fatigue/consistency/why)
    - Solution (clip to block, one lap, stroke-by-stroke metrics on the phone, no laptop)
    - What it measures (3–4 bullets: stroke rate, fatigue, consistency, velocity profile)
    - One shared device per lane; pricing one-liner ($300 device + optional $20/swimmer/mo
      cloud for video + long-term tracking)
    - Early-access CTA (match site: onboarding a few programs first — reply to talk)
    - Sign-off placeholder [Your name]
    Keep it tight (sendable in one screen), coach-voice, no jargon, consistent with the FAQ.
  </action>
  <verify>marketing/sales-pitch-email.md exists with subject + body; pricing + CTA match FAQ and site</verify>
  <done>AC-4 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>FAQ page, updated Pricing + Privacy Policy, and the sales-pitch email.</what-built>
  <how-to-verify>
    1. Run the web dev server; open /faq — read all answers, check the FAQ link in Nav + Footer.
    2. Open homepage Pricing — confirm $300 + $20 model, no "$15" anywhere.
    3. Open /privacy — confirm video disclosure is now accurate and date is bumped.
    4. Open marketing/sales-pitch-email.md — confirm tone/pricing/CTA are right to send.
    5. DECISION FLAG: the privacy change now discloses storing video that may include
       minors (COPPA-sensitive). Confirm you're OK publishing this before attorney review,
       or whether to soften the cloud-tier video wording.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe edits</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any coach-portal code under web/app/app/** (this is marketing + privacy content only)
- Pricing.js section id="pricing" (Nav anchor depends on it)
- Supabase project / service providers list beyond video-storage wording
- The signal pipeline, api.py, iOS app — content-only plan

## SCOPE LIMITS
- No Stripe / checkout wiring (pricing stays informational, per existing site posture)
- No deploy (Vercel auto-deploys on user push — user-owned, as in Phase 27/28)
- No new email provider; the email is a markdown artifact, not an automated send
- No attorney review performed here — flagged as a follow-up only
</boundaries>

<verification>
- [ ] cd web && npm run build  → compiles with no errors
- [ ] /faq renders, no console errors, FAQ link in Nav + Footer
- [ ] Homepage Pricing shows $300 + $20 model; grep web/ for "$15" returns nothing
- [ ] /privacy video disclosure accurate; LAST_UPDATED bumped
- [ ] marketing/sales-pitch-email.md present and consistent
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- FAQ published and linked; sales email artifact created
- Marketing site internally consistent on pricing and video/privacy
- No portal/pipeline/iOS code touched
- Human-verify checkpoint approved (incl. minors'-video disclosure decision)
</success_criteria>

<output>
After completion, create .paul/phases/29-marketing-content/29-01-SUMMARY.md
</output>
