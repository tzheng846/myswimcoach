---
phase: 23-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/** (new Next.js app), web/package.json, web/app/globals.css, web/app/layout.js, web/app/page.js, web/components/marketing/*, web/components/three/DeviceScene.js, web/public/models/README.md, web/src/data/sample-session.json]
autonomous: true
---

<objective>
## Goal
Scaffold the Swimnetics website (Next.js, `web/` folder) and build the complete public marketing site: dark tech-themed landing with a Three.js 3D device hero, how-it-works, features, live sample velocity chart, and informational pricing at $15/swimmer/month.

## Purpose
Replaces the placeholder at `landing/index.html` with the real site for swimnetics.com. Marketing front door for B2B sales (swim academies); also establishes the design system and app shell that plans 23-02/23-03 build the coach portal on.

## Output
- `web/` — Next.js (App Router, JavaScript, Tailwind) project that builds clean
- Marketing landing page at `/` with all sections
- Reusable Three.js device scene with placeholder model + GLB drop-in path
- Design tokens matching the iOS app
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@landing/index.html — copy/voice to carry forward (headline, 3-step flow, early-access CTA, hello@swimnetics.com mailto)
@C:\Users\TonyZheng\Desktop\swimnetics-mobile\src\screens\LoginScreen.js — wave logo + "VELOCITY INTELLIGENCE" branding to match
@processed\ — real session CSVs (time_s, dist_m, vel_ms, accel_ms2 @ 100 Hz) for the sample chart

## Design tokens (extracted from iOS app + Phase 18 mockup — use these exactly)
- Background: `#07090E` (page), surfaces `#1A1A1A` / `#252525` / `#2A2A2A`
- Primary: `#2196F3` (electric blue), secondary `#2563EB`, deep navy `#1E3A5F`
- Text: `#F0F2F5` primary, `#7F8C8D` muted, `#B0B8C4` secondary
- Status: green `#27AE60`, amber `#F39C12` / `#E67E22`, red `#C0392B`
- Font: system stack (-apple-system, Segoe UI…); generous letter-spacing on uppercase labels (iOS uses small uppercase section labels)
- Aesthetic: sleek, elegant, tech — restrained. Thin borders (`#1E3A5F`-tinted), subtle blue glow accents, NO neon overload, no busy gradients. Think Linear/Vercel landing pages, not crypto.
</context>

<acceptance_criteria>

## AC-1: Marketing site renders with matching design language
```gherkin
Given the Next.js dev server is running
When a visitor opens /
Then they see a dark-themed (#07090E) landing page with hero, how-it-works (3 steps), features, sample chart, pricing, and footer sections
And colors/typography match the design tokens above
And the layout is responsive (usable at 390px and 1440px widths)
```

## AC-2: 3D device hero works without assets
```gherkin
Given no GLB file exists at web/public/models/device.glb
When the hero section loads
Then a Three.js scene renders a stylized placeholder device (encoder wheel + housing + tether) with slow idle rotation and pointer parallax
And when a device.glb is later placed at that path, it loads instead of the placeholder with no code change
```

## AC-3: Pricing reflects new model
```gherkin
Given a visitor scrolls to the pricing section
When they read it
Then pricing shows $15/swimmer/month (informational, no checkout)
And the CTA is "Get early access" → mailto:hello@swimnetics.com
And there is no mention of the old $200/$1,000 tiers anywhere on the site
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js app + design system</name>
  <files>web/ (create-next-app output), web/app/globals.css, web/app/layout.js, web/components/Nav.js, web/components/Footer.js</files>
  <action>
    Run `npx create-next-app@latest web` with: JavaScript (NOT TypeScript — repo is JS),
    App Router, Tailwind CSS, ESLint, no src/ dir is fine (accept default), import alias @/*.
    Define the design tokens as CSS variables in globals.css and wire into Tailwind theme
    (colors.bg, colors.surface, colors.primary, etc.). Build shared layout: sticky top nav
    (Swimnetics wordmark left — wave-style mark consistent with iOS LoginScreen; links:
    How it works, Features, Pricing, "Coach Login" button placeholder linking to /login)
    and footer (brand, "Built for swim academies and competitive programs", contact mailto).
    Set metadata (title, description) carried from landing/index.html.
    Avoid: TypeScript, CSS-in-JS libs, component libraries (MUI/Chakra) — Tailwind only.
  </action>
  <verify>`npm run dev` in web/ serves; / renders nav + footer with dark theme; `npm run build` exits 0</verify>
  <done>AC-1 partially satisfied: shell + design system in place</done>
</task>

<task type="auto">
  <name>Task 2: Marketing page sections</name>
  <files>web/app/page.js, web/components/marketing/Hero.js, web/components/marketing/HowItWorks.js, web/components/marketing/Features.js, web/components/marketing/SampleChart.js, web/components/marketing/Pricing.js, web/src/data/sample-session.json</files>
  <action>
    Build the landing page sections:
    - Hero: headline "Stroke-level feedback, delivered poolside — in seconds." + lede from
      landing/index.html, primary CTA "Get early access" (mailto), reserved right/back slot
      for the 3D scene (Task 3 fills it).
    - HowItWorks: 3 steps (Clip it on / Swim the set / See it instantly) — carry copy from
      landing/index.html, restyle as dark cards with numbered accents.
    - Features: grid of metric capabilities drawn from metrics.py session keys — stroke rate,
      DPS, fatigue index, velocity consistency (CV), coast fraction, time-to-distance,
      data-quality flags. Each: icon (inline SVG, simple line icons), name, one-line coach-value
      description. Plus a row for: iOS recording app, instant server processing (~30 s),
      session history per athlete.
    - SampleChart: client component rendering a real velocity trace. Generate
      web/src/data/sample-session.json by downsampling one breaststroke processed CSV
      (e.g. processed/swim_lucas_br_2_*.csv) to ≤600 points of {t, v} with a small Python
      one-liner during the build of this task (run it once, commit the JSON — no runtime
      Python dependency). Render with Recharts (add dependency): blue line on dark grid,
      annotate one stroke cycle (peak = arm pull, trough = glide) with subtle labels.
    - Pricing: single card — "$15 / swimmer / month", subline "per active swimmer, billed to
      the program", bullet list (unlimited sessions, all metrics, iOS app + web portal,
      shared encoder hardware), CTA "Get early access" mailto. Informational only — NO
      checkout, NO old $200/$1,000 tiers.
    Avoid: lorem ipsum (use real product copy), stock-photo aesthetics, fake testimonials.
  </action>
  <verify>/ shows all sections in order; sample chart draws a real velocity curve; grep -ri "200/mo\|1,000\|1000/mo" web/app web/components returns nothing</verify>
  <done>AC-1 and AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Three.js device hero with GLB drop-in</name>
  <files>web/components/three/DeviceScene.js, web/components/three/PlaceholderDevice.js, web/public/models/README.md</files>
  <action>
    Add dependencies: three, @react-three/fiber, @react-three/drei.
    DeviceScene: client component (`dynamic(..., { ssr: false })` import in Hero) rendering
    a Canvas with: placeholder device built from primitives — flat cylinder (encoder wheel,
    dark metal material with blue rim-light), rounded box housing, thin curved line for the
    tether trailing off-frame; slow idle Y-rotation; pointer-driven parallax tilt (small,
    elegant — max ~6°); soft studio lighting (ambient + two directional, blue-tinted fill);
    transparent canvas background so the page bg shows through.
    GLB drop-in: attempt `useGLTF('/models/device.glb')` inside an error boundary /
    Suspense; if the file is absent fall back to PlaceholderDevice. Document in
    web/public/models/README.md: export from Fusion 360 as glTF/GLB (mesh, not STEP),
    suggested scale/orientation (+Y up, wheel axis on Z), drop file here — no code change.
    Respect prefers-reduced-motion: freeze idle rotation when set.
    Avoid: heavy postprocessing (bloom passes), OrbitControls (no user dragging), loading
    spinners that flash — render placeholder immediately.
  </action>
  <verify>Hero renders the 3D scene with no console errors and no hydration mismatch; placing any valid .glb at web/public/models/device.glb renders it instead (test with a trivial exported cube if available, else verify the fallback path logic by code review + absent-file behavior)</verify>
  <done>AC-2 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- landing/index.html (stays live until the new site is deployed)
- api.py, metrics.py, vel_acc_extraction.py, app.py (no backend changes in this plan)
- ESP_32_V5/, *_esp32/ firmware folders
- C:\Users\TonyZheng\Desktop\swimnetics-mobile (iOS repo — read-only reference)
- .paul/phases/22-* (open plan 22-02 untouched)

## SCOPE LIMITS
- No auth, no Supabase, no portal pages in this plan (23-02)
- No Stripe/checkout — pricing is informational text only
- No deployment (Vercel config in 23-03)
- No TypeScript migration, no CMS, no analytics/tracking scripts

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` in web/ exits 0 with no errors
- [ ] / renders all sections at 390px and 1440px (preview tools)
- [ ] 3D scene renders, falls back gracefully without device.glb
- [ ] Pricing shows $15/swimmer/month only
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed, all verification checks pass
- Marketing site is visually polished — dark, sleek, restrained tech theme matching iOS palette
- Zero references to old pricing tiers
</success_criteria>

<output>
After completion, create `.paul/phases/23-website/23-01-SUMMARY.md`
</output>
